# gitops/clusters/staging/argocd/kustomization.yaml
# Composes the sub-bases below (same pattern as clusters/production/argocd/).
# Staging has no notifications/ sub-base — Argo CD Notifications -> marketplace Slack
# is production-only (see gitops/providers/both/production/kustomization.yaml).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - core
  - app-of-apps
  - rollouts
