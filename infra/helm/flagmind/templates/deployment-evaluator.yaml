{{- if .Values.evaluator.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "tombstone.fullname" . }}-evaluator
  labels:
    {{- include "tombstone.labels" . | nindent 4 }}
    app.kubernetes.io/component: evaluator
spec:
  {{- if not .Values.evaluator.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "tombstone.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: evaluator
  template:
    metadata:
      labels:
        {{- include "tombstone.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: evaluator
    spec:
      serviceAccountName: {{ include "tombstone.serviceAccountName" . }}
      terminationGracePeriodSeconds: 30
      securityContext:
        {{- include "tombstone.podSecurityContext" . | nindent 8 }}
      {{- include "tombstone.topologySpreadConstraints" (dict "component" "evaluator" "context" $) | nindent 6 }}
      containers:
        - name: evaluator
          image: "{{ .Values.global.imageRegistry }}{{ .Values.evaluator.image.repository }}:{{ .Values.evaluator.image.tag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          securityContext:
            {{- include "tombstone.containerSecurityContextBase" . | nindent 12 }}
            readOnlyRootFilesystem: {{ .Values.evaluator.readOnlyRootFilesystem }}
          ports:
            - name: http
              containerPort: {{ .Values.evaluator.service.port }}
              protocol: TCP
          startupProbe:
            httpGet:
              path: /health
              port: {{ .Values.evaluator.service.port }}
            periodSeconds: 5
            failureThreshold: 12
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.evaluator.service.port }}
            initialDelaySeconds: 10
            periodSeconds: 15
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /readyz
              port: {{ .Values.evaluator.service.port }}
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
          envFrom:
            - configMapRef:
                name: tombstone-config
            - secretRef:
                name: tombstone-secrets
          resources:
            requests:
              cpu: {{ .Values.evaluator.resources.requests.cpu }}
              memory: {{ .Values.evaluator.resources.requests.memory }}
            limits:
              cpu: {{ .Values.evaluator.resources.limits.cpu }}
              memory: {{ .Values.evaluator.resources.limits.memory }}
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
{{- end }}
