{{- if .Values.flagApi.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "tombstone.fullname" . }}-flag-api
  labels:
    {{- include "tombstone.labels" . | nindent 4 }}
    app.kubernetes.io/component: flag-api
spec:
  {{- if not .Values.flagApi.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "tombstone.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: flag-api
  template:
    metadata:
      labels:
        {{- include "tombstone.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: flag-api
    spec:
      serviceAccountName: {{ include "tombstone.serviceAccountName" . }}
      terminationGracePeriodSeconds: 30
      securityContext:
        {{- include "tombstone.podSecurityContext" . | nindent 8 }}
      {{- include "tombstone.topologySpreadConstraints" (dict "component" "flag-api" "context" $) | nindent 6 }}
      containers:
        - name: flag-api
          image: "{{ .Values.global.imageRegistry }}{{ .Values.flagApi.image.repository }}:{{ .Values.flagApi.image.tag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          securityContext:
            {{- include "tombstone.containerSecurityContextBase" . | nindent 12 }}
            readOnlyRootFilesystem: {{ .Values.flagApi.readOnlyRootFilesystem }}
          ports:
            - name: http
              containerPort: {{ .Values.flagApi.service.port }}
              protocol: TCP
          startupProbe:
            httpGet:
              path: /health
              port: {{ .Values.flagApi.service.port }}
            periodSeconds: 5
            failureThreshold: 12
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.flagApi.service.port }}
            initialDelaySeconds: 10
            periodSeconds: 15
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.flagApi.service.port }}
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
          envFrom:
            - configMapRef:
                name: tombstone-config
            - secretRef:
                name: tombstone-secrets
          resources:
            requests:
              cpu: {{ .Values.flagApi.resources.requests.cpu }}
              memory: {{ .Values.flagApi.resources.requests.memory }}
            limits:
              cpu: {{ .Values.flagApi.resources.limits.cpu }}
              memory: {{ .Values.flagApi.resources.limits.memory }}
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
{{- end }}
