{{- if .Values.marketplace.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "tombstone.fullname" . }}-marketplace
  labels:
    {{- include "tombstone.labels" . | nindent 4 }}
    app.kubernetes.io/component: marketplace
spec:
  {{- if not .Values.marketplace.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "tombstone.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: marketplace
  template:
    metadata:
      labels:
        {{- include "tombstone.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: marketplace
    spec:
      serviceAccountName: {{ include "tombstone.serviceAccountName" . }}
      terminationGracePeriodSeconds: 30
      securityContext:
        {{- include "tombstone.podSecurityContext" . | nindent 8 }}
      {{- include "tombstone.topologySpreadConstraints" (dict "component" "marketplace" "context" $) | nindent 6 }}
      containers:
        - name: marketplace
          image: "{{ .Values.global.imageRegistry }}{{ .Values.marketplace.image.repository }}:{{ .Values.marketplace.image.tag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          securityContext:
            {{- include "tombstone.containerSecurityContextBase" . | nindent 12 }}
            readOnlyRootFilesystem: {{ .Values.marketplace.readOnlyRootFilesystem }}
          ports:
            - name: http
              containerPort: {{ .Values.marketplace.service.port }}
              protocol: TCP
          startupProbe:
            httpGet:
              path: /health
              port: {{ .Values.marketplace.service.port }}
            periodSeconds: 5
            failureThreshold: 12
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.marketplace.service.port }}
            initialDelaySeconds: 10
            periodSeconds: 15
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /readyz
              port: {{ .Values.marketplace.service.port }}
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
          envFrom:
            - configMapRef:
                name: tombstone-config
            - secretRef:
                name: tombstone-secrets
          resources:
            requests:
              cpu: {{ .Values.marketplace.resources.requests.cpu }}
              memory: {{ .Values.marketplace.resources.requests.memory }}
            limits:
              cpu: {{ .Values.marketplace.resources.limits.cpu }}
              memory: {{ .Values.marketplace.resources.limits.memory }}
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
{{- end }}
