[project]
name = "tombstone"
version = "0.1.0"
description = "FlagMind Python SDK — server-side feature flag evaluation"
requires-python = ">=3.10"
dependencies = ["httpx>=0.27.0", "mmh3>=4.0.0"]

[tool.uv]
dev-dependencies = ["pytest>=8.0.0"]
